# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (never commit secrets — service role key, Dintero/Gelato keys)
.env
.env*.local
.env.local

# Provider credentials arrive as CSV exports from Dintero and Gelato. One was
# committed in 11d8223 and only removed in c07e464, so it sat in the history of a
# public repo. Patterns rather than a single filename: the export is named after the
# account, so the next one has a different name.
*credential*
*.csv
*.key

# typescript
*.tsbuildinfo

# supabase local
/supabase/.branches
/supabase/.temp


